[package]
name = "starfield"
version = "0.16.2"
edition = "2021"
authors = ["Matthew Goodman"]
description = "Astronomical data reduction toolkit with star catalogs, coordinate systems, and star finding algorithms (inspired by skyfield)"
repository = "https://github.com/OrbitalCommons/starfield"
license = "MIT"
readme = "README.md"
keywords = ["astronomy", "catalog", "stars", "celestial", "skyfield"]
categories = ["science"]
exclude = [
    "test_data/",
    "python-skyfield/",
    "devops/",
    ".env",
    ".env.python",
    ".vscode/",
    ".python-version",
    ".skyfield-version",
]

[dependencies]
# Core astronomical calculations
nalgebra = "0.32"           # Linear algebra
time = "0.3"                # Time handling
num = "0.4"                 # Numerical types
lazy_static = "1.4"         # Lazy initialization
ndarray = "0.17"            # N-dimensional arrays

# Data handling
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"

# Network and compression
reqwest = { version = "0.12", features = ["blocking", "json", "multipart"] }
flate2 = "1.0"                                          # GZip compression
rand = "0.9"                                            # Random number generation for synthetic data
md5 = "0.7"                                             # MD5 checksum calculation
regex = "1.10"                                          # Regular expressions for parsing
byteorder = "1.5"                                       # Binary data reading/writing

# Error handling and utilities
thiserror = "2.0"
log = "0.4"
chrono = "0.4.40"
base64 = "0.22.1"
indicatif = "0.17.11"  # Progress bars and indicators

# Image processing
# Minimal feature set: AVIF (default) drags in rav1e → {core2, rand 0.9, paste}
# — three of cargo-audit's warnings come from there and we don't decode AVIF.
# Consumers that need additional formats can enable them in their own deps.
image = { version = "0.25", default-features = false, features = ["png", "jpeg"] }

# Python comparison tests
# These dependencies are only active when the python-tests feature is enabled
anyhow = { version = "1.0", optional = true }
pyo3 = { version = "0.24", optional = true } # Python bindings for testing against Python implementation
once_cell = "1.21.3"
clap = { version = "4.5.40", features = ["derive"] }
memmap2 = "0.9"
uom = "0.37.0"
sgp4 = "2.3.0"
starfield-datastore = { version = "0.1", optional = true }


[dev-dependencies]
criterion = "0.5" # Benchmarking
approx = "0.5"    # Approximate equality assertions
rstest = "0.18"   # Test fixtures and parameterization
tempfile = "3.8"  # Temporary file creation for tests
# Dev dependencies

[features]
default = ["datastore"]
python-tests = ["pyo3", "anyhow"]
# Resolve SPICE kernels through the starfield-datastore pull-through cache
# (local disk -> organisation mirror -> upstream archive) instead of fetching
# them straight from JPL and NAIF. Default on; turn it off for a build that
# talks to the archives directly.
datastore = ["dep:starfield-datastore"]
# Optional traits for per-band photometry, measured radial surface-brightness
# profiles, and isophote shape series. Gated behind a feature so consumers
# that only need positions/magnitudes don't pay for the larger trait surface.
photometry = []
